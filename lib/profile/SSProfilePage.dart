/*
 * Author : Johnny Cheung
 * Page: 个人页
 */

import 'package:flutter/material.dart';

class SSProfilePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Profile'),
    );
  }
}
